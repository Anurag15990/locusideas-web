<div class="col-md-4 portfolio-item">
    <a href="#">
        <img class="img-responsive" src="{{project.medias.initial.firstObject.media.url}}" alt="">
    </a>
    <h3>
        <a href="#">{{project.title}}</a>
    </h3>
    <p>{{project.description}}</p>
</div>
{{yield}}
